function [feature_vec] = generateData(obj, input)
    feature_vec = [];
    if ismember('bSQI',obj.feature_names)
        % TODO: implement features
    end
    
    if ismember('kurtosis',obj.feature_names)
    end
    
    if ismember('skewness',obj.feature_names)
    end
    
    if ismember('pSQI',obj.feature_names)
    end
end
